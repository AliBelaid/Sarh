# Sarh — edge vhosts. Five hostnames terminate here:
#
#   sarh.ly             → web-citizen (citizen portal)
#   verify.sarh.ly      → public deed verification SPA + /verify/* API
#   officer.sarh.ly     → web-officer
#   issuer.sarh.ly      → web-id-issuer
#   admin.sarh.ly       → web-admin
#
# All five share the same /api/v1 reverse proxy to the NestJS service.
# All five upgrade plain HTTP to HTTPS with HSTS preload.

# ---- Upstreams -------------------------------------------------------------
# Resolved against the docker-compose network; each name is a service.
upstream sarh_api      { server api:3000          max_fails=3 fail_timeout=10s; keepalive 32; }
upstream sarh_citizen  { server web-citizen:80    max_fails=3 fail_timeout=10s; keepalive 16; }
upstream sarh_officer  { server web-officer:80    max_fails=3 fail_timeout=10s; keepalive 16; }
upstream sarh_issuer   { server web-id-issuer:80  max_fails=3 fail_timeout=10s; keepalive 16; }
upstream sarh_admin    { server web-admin:80      max_fails=3 fail_timeout=10s; keepalive 16; }

# ---- Common header maps ----------------------------------------------------
# Lift Connection through to upstream so keepalive works.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# CSP — separate maps per app so we can relax for the verify site (Mapbox
# tiles + GeoJSON inlined) without loosening the others.
map $host $sarh_csp {
    default                 "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: blob:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'; upgrade-insecure-requests";
    verify.sarh.ly       "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://api.mapbox.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: blob: https://*.tiles.mapbox.com https://api.mapbox.com; connect-src 'self' https://api.mapbox.com https://*.tiles.mapbox.com; worker-src 'self' blob:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'; upgrade-insecure-requests";
    issuer.sarh.ly       "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: blob:; connect-src 'self' http://127.0.0.1:9090 ws://127.0.0.1:9090; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'";
}

# ---- Boilerplate proxy block (re-included by every location that proxies) -
# /etc/nginx/proxy_params ships in nginx:alpine; we override here so the
# config is self-contained and immune to upstream image changes.

# ---- 1) HTTP → HTTPS redirect for every Sarh host ----------------------
server {
    listen      80;
    listen      [::]:80;
    server_name sarh.ly verify.sarh.ly officer.sarh.ly issuer.sarh.ly admin.sarh.ly;

    # ACME http-01 challenges from certbot land here. Paths under
    # /.well-known/acme-challenge are served from a shared volume.
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# ---- 2) Citizen portal — sarh.ly ---------------------------------------
server {
    listen              443 ssl;
    listen              [::]:443 ssl;
    http2               on;
    server_name         sarh.ly;

    ssl_certificate     /etc/letsencrypt/live/sarh.ly/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sarh.ly/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/sarh.ly/chain.pem;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options    "nosniff"                                       always;
    add_header X-Frame-Options           "DENY"                                          always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin"               always;
    add_header Permissions-Policy        "camera=(), microphone=(), geolocation=(self), usb=()" always;
    add_header Content-Security-Policy   $sarh_csp                                    always;
    add_header Cross-Origin-Opener-Policy "same-origin"                                  always;

    limit_conn conn_zone 50;

    include /etc/nginx/conf.d/_api_proxy.inc;

    location / {
        proxy_pass http://sarh_citizen;
        include /etc/nginx/conf.d/_proxy_common.inc;
    }
}

# ---- 3) Public verify portal — verify.sarh.ly --------------------------
server {
    listen              443 ssl;
    listen              [::]:443 ssl;
    http2               on;
    server_name         verify.sarh.ly;

    ssl_certificate     /etc/letsencrypt/live/verify.sarh.ly/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/verify.sarh.ly/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/verify.sarh.ly/chain.pem;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options    "nosniff"                                       always;
    add_header X-Frame-Options           "DENY"                                          always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin"               always;
    add_header Permissions-Policy        "camera=(), microphone=(), geolocation=()"      always;
    add_header Content-Security-Policy   $sarh_csp                                    always;
    add_header Cross-Origin-Opener-Policy "same-origin"                                  always;

    limit_conn conn_zone 100;

    # /verify/* is the unauthenticated lookup endpoint. CLAUDE.md mandates
    # rate limiting it — burst=20 nodelay lets a quick page-load fan out
    # cleanly without batching, while the 30r/m steady state stops scraping.
    location ~ ^/(api/v1/)?verify/ {
        limit_req zone=verify_zone burst=20 nodelay;
        proxy_pass http://sarh_api;
        include /etc/nginx/conf.d/_proxy_common.inc;
    }

    include /etc/nginx/conf.d/_api_proxy.inc;

    # The verify portal is served by the citizen Angular app (it ships a
    # /verify route). Routing the host to the same upstream means we don't
    # need a separate container.
    location / {
        proxy_pass http://sarh_citizen;
        include /etc/nginx/conf.d/_proxy_common.inc;
    }
}

# ---- 4) Officer portal — officer.sarh.ly -------------------------------
server {
    listen              443 ssl;
    listen              [::]:443 ssl;
    http2               on;
    server_name         officer.sarh.ly;

    ssl_certificate     /etc/letsencrypt/live/officer.sarh.ly/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/officer.sarh.ly/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/officer.sarh.ly/chain.pem;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options    "nosniff"                                       always;
    add_header X-Frame-Options           "DENY"                                          always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin"               always;
    add_header Permissions-Policy        "camera=(), microphone=(), geolocation=(self)"  always;
    add_header Content-Security-Policy   $sarh_csp                                    always;
    add_header Cross-Origin-Opener-Policy "same-origin"                                  always;

    limit_conn conn_zone 50;

    include /etc/nginx/conf.d/_api_proxy.inc;

    location / {
        proxy_pass http://sarh_officer;
        include /etc/nginx/conf.d/_proxy_common.inc;
    }
}

# ---- 5) ID issuer station — issuer.sarh.ly -----------------------------
# This vhost is browsed from kiosks on the office LAN; it still terminates
# TLS publicly so officers can also access from laptops with VPN.
server {
    listen              443 ssl;
    listen              [::]:443 ssl;
    http2               on;
    server_name         issuer.sarh.ly;

    ssl_certificate     /etc/letsencrypt/live/issuer.sarh.ly/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/issuer.sarh.ly/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/issuer.sarh.ly/chain.pem;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options    "nosniff"                                       always;
    add_header X-Frame-Options           "DENY"                                          always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin"               always;
    # Camera + USB needed for live photo capture and ACR122U over WebUSB.
    add_header Permissions-Policy        "camera=(self), microphone=(), geolocation=(), usb=(self)" always;
    add_header Content-Security-Policy   $sarh_csp                                    always;
    add_header Cross-Origin-Opener-Policy "same-origin"                                  always;
    # COEP+COOP enable SharedArrayBuffer used by the local NFC helper.
    add_header Cross-Origin-Embedder-Policy "credentialless"                              always;

    # Allow larger uploads here — issuer station posts photo + signature.
    client_max_body_size 30m;

    limit_conn conn_zone 30;

    include /etc/nginx/conf.d/_api_proxy.inc;

    location / {
        proxy_pass http://sarh_issuer;
        include /etc/nginx/conf.d/_proxy_common.inc;
    }
}

# ---- 6) Admin console — admin.sarh.ly ----------------------------------
server {
    listen              443 ssl;
    listen              [::]:443 ssl;
    http2               on;
    server_name         admin.sarh.ly;

    ssl_certificate     /etc/letsencrypt/live/admin.sarh.ly/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/admin.sarh.ly/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/admin.sarh.ly/chain.pem;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options    "nosniff"                                       always;
    add_header X-Frame-Options           "DENY"                                          always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin"               always;
    add_header Permissions-Policy        "camera=(), microphone=(), geolocation=()"      always;
    add_header Content-Security-Policy   $sarh_csp                                    always;
    add_header Cross-Origin-Opener-Policy "same-origin"                                  always;

    limit_conn conn_zone 20;

    include /etc/nginx/conf.d/_api_proxy.inc;

    location / {
        proxy_pass http://sarh_admin;
        include /etc/nginx/conf.d/_proxy_common.inc;
    }
}
