# Shell scripts and git hooks MUST stay LF, or they fail with "bad interpreter"
# on Unix and in git-bash. Keep this narrow to avoid repo-wide renormalization.
.githooks/* text eol=lf
*.sh        text eol=lf
